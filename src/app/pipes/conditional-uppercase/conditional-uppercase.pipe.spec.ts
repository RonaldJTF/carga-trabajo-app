import { ConditionalUppercasePipe } from './conditional-uppercase.pipe';

describe('ConditionalUppercasePipe', () => {
  it('create an instance', () => {
    const pipe = new ConditionalUppercasePipe();
    expect(pipe).toBeTruthy();
  });
});
