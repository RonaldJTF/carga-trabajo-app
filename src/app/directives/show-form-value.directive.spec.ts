import { ShowFormValueDirective } from './show-form-value.directive';

describe('ShowFormValueDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowFormValueDirective();
    expect(directive).toBeTruthy();
  });
});
